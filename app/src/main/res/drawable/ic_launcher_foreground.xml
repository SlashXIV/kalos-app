<?xml version="1.0" encoding="utf-8"?>
<!--
    Foreground adaptive icon — bitmap pré-paddé à 18% de chaque côté
    Artwork à 64% du canvas (108dp) = 69dp → dans la safe zone 72dp (3dp de marge)
    PNG simple (pas de layer-list) → chargement atomique, pas de flicker sur MIUI
-->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/ic_launcher_foreground_padded"
    android:gravity="fill" />
