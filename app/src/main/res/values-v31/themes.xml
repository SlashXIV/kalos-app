<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31+) splash screen
    windowSplashScreenIconBackgroundColor = splash_background (#09090F)
    → fond du cercle = fond général du splash → le cercle est INVISIBLE
    → le K blanc apparaît directement sur le fond sombre, rendu épuré et premium
    → plus de carré/cercle gris technique visible au lancement
-->
<resources>
    <style name="Theme.Kalos" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Kalos.NoActionBar</item>
    </style>

    <style name="Theme.Kalos.NoActionBar" parent="android:Theme.Material.NoActionBar" />
</resources>
